# hybrid_knot_indexer Test Data

The files below `che_data/` are copied without content changes from:

- Repository: <https://github.com/TopologicalKnotIndexer/hybrid_knot_indexer>
- Commit: `b959fda15f76ab7bf6eb02571a5dbd237024b65b`
- Upstream path: `src/che_data/`
- Included cases: 22

The data is kept in this repository so the default compatibility test is
offline, deterministic, and independent of upstream availability. The test
runner can still exercise another upstream revision with `--upstream` or use a
local checkout with `--source`.
